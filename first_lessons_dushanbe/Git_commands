git checkout main - переключается в основной бранч
git pull - вытягивает из облака в локальную версию

git checkout -b имя_бранча

написать функцию которая принимает два параметра - массив чисел и число, которое нужно найти в массиве
 и она должна возвратить индекс в массиве если она найдена или -1 если не найден.